// Imports

import express from "express";
import fs from "fs-extra";

// Presets

let editRouter = express.Router();

// Code

editRouter.patch("/", (req, res) => {});

// Exports

export default editRouter;
